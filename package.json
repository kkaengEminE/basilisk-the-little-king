{
  "name": "basilisk-the-little-king",
  "version": "0.1.0",
  "private": true,
  "description": "BASILISK: The Little King — a medieval-bestiary survival roguelike (snake growth + Vampire Survivors progression).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
